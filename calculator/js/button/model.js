var ButtonModel = function(id, value, options){
    this.id= id;
    this.value = value;
    this.options = {
        width : options.width ,
        height : options.height,
        color : options.color ,
        className : options.className
    }
}
